# TEST-PLAN.md — 테스트 전략

> 상태: v0.7 (D-075 — 한국 공제조합 연동·E-Wallet·글로벌 결제: §2.14 한국 공제조합 연동·E-Wallet·글로벌 결제 테스트 추가, §3/§4 갱신. D-074 — Dynamic Board Engine: §2.13 Dynamic Board Engine 테스트 추가, §3/§4 갱신. D-073 — 운영 UX 및 고객 경험 완성: §2.12 운영 UX 및 고객 경험 테스트 추가, §3/§4 갱신. D-072 — 쇼핑몰 UX·알림·운영자 대시보드 완성: §2.11 쇼핑몰 UX/알림/운영자 대시보드 테스트 추가, §3/§4 갱신. D-070 — 쇼핑몰 운영 Phase 2 및 문서 동기화: §2.10 쇼핑몰 운영 Phase 2/SEO/Digital Marketing 테스트 추가, §3/§4 갱신. D-064 — 개발착수 전 최종 안정화: §2.8 ERP Core 엔진 테스트, §2.9 Multi-Tenant 격리 테스트 추가) · 최종 수정일: 2026-06-26 · 단계: 설계(Design)
> 전제 문서: [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md), [ARCHITECTURE.md](ARCHITECTURE.md), [COMPENSATION-RULES.md](COMPENSATION-RULES.md), [SETTLEMENT-RULES.md](SETTLEMENT-RULES.md), [DATABASE.md](DATABASE.md)

## 0. 문서의 목적과 범위

본 문서는 FNS(Multi-Tenant MLM ERP) 구현 단계에서 **무엇을 어떤 레벨로 테스트할지**를 정의한다. [COMPENSATION-RULES.md](COMPENSATION-RULES.md)/[SETTLEMENT-RULES.md](SETTLEMENT-RULES.md)에 정의된 계산 로직 자체를 변경하거나 재정의하지 않으며, 오직 "그 로직이 설계대로 동작하는지 어떻게 검증할지"만 다룬다. 아직 코드가 없는 설계 단계이므로, 본 문서의 테스트 케이스는 **구현 착수 시 그대로 테스트 스위트로 옮길 수 있는 명세** 역할을 한다.

후속 변경 시 [DECISIONS.md](DECISIONS.md)에 결정 기록을 남기고 본 문서를 갱신한다 — [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md) §1.1의 문서 변경 원칙과 동일하게 취급한다.

## 1. 테스트 레벨과 비율 권장안

| 레벨 | 책임 범위 | 권장 비율 | 근거 |
|---|---|---|---|
| **Unit** | 순수 계산 함수 단위 — 라인 깊이 판정, 비율 적용, 페어링 큐 로직, 35% 비율 계산, 세금 원천징수액 계산 등 입력→출력이 결정적인 로직 | **높음 (전체 테스트의 다수 비중)** | [COMPENSATION-RULES.md](COMPENSATION-RULES.md)/[SETTLEMENT-RULES.md](SETTLEMENT-RULES.md)는 "회사의 실제 자금이 지급되는 핵심 비즈니스 로직"([COMPENSATION-RULES.md](COMPENSATION-RULES.md) 서두 경고)이며, [ARCHITECTURE.md](ARCHITECTURE.md) §2.2는 Compensation/Settlement가 "입력 스냅샷을 받아 순수 계산을 수행하도록 설계"되어 있다고 명시한다 — 순수 함수는 회귀위험이 큰 만큼 Unit 레벨에서 가장 값싸게, 가장 빠르게, 가장 많은 경우의 수로 검증 가능하다 |
| **Integration** | worker가 Supabase PostgreSQL에 실제로 쓰는 흐름 — Job 생성→큐 적재→worker 소비→append-only 원장 기록까지의 연계, api가 Job 상태를 올바르게 조회하는지, 국가별 마케팅 플랜 버전 조회가 올바른 시점의 버전을 가져오는지 | **중간** | [ARCHITECTURE.md](ARCHITECTURE.md) §1.1의 "계산과 요청처리의 분리 원칙"은 서비스 간 경계(api↔worker↔redis↔Postgres)에서만 깨질 수 있으므로, 이 경계를 넘는 흐름은 Unit으로 잡을 수 없고 Integration이 필요하다 |
| **E2E** | 사용자 관점 흐름 전체 — 주문 생성부터 정산 지급까지, 회원가입부터 조직 이동 적용까지 | **낮음 (핵심 critical path만 소수)** | E2E는 느리고 깨지기 쉬워 회귀 탐지 비용이 높다. [ARCHITECTURE.md](ARCHITECTURE.md)가 정의하는 5개 서비스 흐름이 "한 줄로 끝까지 이어지는지"를 확인하는 용도로만 최소한으로 둔다 |

**원칙**: 계산 로직(MLM/정산/35% 게이트)일수록 Unit 비중을 극단적으로 높이고, 서비스 경계를 넘는 흐름일수록 Integration/E2E로 내려간다. 이는 [ARCHITECTURE.md](ARCHITECTURE.md) §1.1이 강제하는 "계산과 요청처리의 물리적 분리" 구조와 정확히 대응된다 — 계산은 worker 내부의 순수 함수이므로 Unit으로 격리해서 검증할 수 있고, 검증해야 한다.

## 2. 영역별 테스트 전략

### 2.1 MLM/수당 계산 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | LINE1~5 라인별 도달깊이 판정(D-018), 라인 단일비율(3%/4%/5%) 적용, 다중 라인 합산, 자격 판정(§3.5.2 매월 5만원 / §3.5.5 패키지 구매 자격), 패키지 정책별 유니레벨 포함 여부(`counts_toward_unilevel_line_revenue`) 반영 |
| **왜** | [DATABASE.md](DATABASE.md) §4 원칙 2 "모든 계산 결과는 입력 스냅샷을 함께 저장하여 사후 검증 가능하게 한다"는 설계가 이미 "동일 입력 → 동일 출력" 회귀 테스트를 1급 시민으로 지원하도록 만들어져 있다 — `commission_records`에 저장된 입력 스냅샷을 그대로 재생(replay)하면 같은 출력이 나와야 한다. 또한 [COMPENSATION-RULES.md](COMPENSATION-RULES.md) §3.3은 D-018에서 "LINE별로 독립 계산해 합산(최대 18%)" 방식이 회사 원본 문서와 불일치해 폐기되었다고 명시한다 — 이런 회귀가 한 번 실제로 발생했던 만큼, 동일 클래스의 오류가 재발하지 않도록 worked-example 기반 테스트가 필수다 |
| **어떻게(레벨)** | **Unit 다수**: §3.3의 검증 예시(라인 1,555명, 평균 구매 50,000원, 월매출 77,750,000원 × 5% = 3,887,500원)를 그대로 테스트 케이스로 사용 — "라인별로 3%+3%+3%+4%+5%를 합산하면 3,736,500원이 되어 원본과 달라진다"는 D-018의 반례도 함께 negative 테스트로 등록해, 향후 누군가 실수로 D-014 방식(라인별 독립 합산)으로 되돌리면 즉시 실패하도록 한다. **Unit**: 스냅샷 재생 테스트 — 동일한 `commission_records.input_snapshot`을 계산 함수에 다시 넣었을 때 저장된 결과와 100% 일치하는지 확인(회귀 감지용, CI에 상시 포함). **Integration**: 탈퇴(WITHDRAWN) 회원이 "다른 회원의 라인 깊이 판정에는 포함되지만 본인이 수령자가 되는 계산에서는 제외"되는지(D-021, [COMPENSATION-RULES.md](COMPENSATION-RULES.md) §3.3) — 수령 대상 필터링과 트리 순회가 서로 다른 단계임을 worker 레벨에서 검증 |

#### Worked-example 테스트 케이스 표 (D-018 핵심 로직)

| 케이스 | 입력 | 기대 출력 | 검증 포인트 |
|---|---|---|---|
| 정상 케이스(원본 문서) | 라인 1개, LINE1~5 가득 찬 6진 트리(1,555명), 평균 구매 50,000원 | 월매출 77,750,000원 × 5% = 3,887,500원 | 라인 전체에 "도달 최대 깊이(LINE5)" 기준 단일비율(5%) 적용 |
| D-014 회귀 방지(negative) | 위와 동일 입력 | **3,736,500원이 나오면 실패** (3%+3%+3%+4%+5% 라인별 합산 방식의 결과값) | 라인별 독립 계산·합산 방식으로 회귀하지 않았는지 |
| LINE1~3만 도달 | 라인이 LINE3까지만 형성 | 적용 비율 3% | "3단계 이하 = 3%" 구간 경계값 |
| LINE6 이상 존재 | 라인이 LINE6까지 형성 | LINE6 매출은 산정에서 제외, 비율은 LINE5 도달 기준 5%로 동일 | "LINE6 이상은 어떤 라인에서도 산정에 포함하지 않는다"(확정) 검증 |
| 다중 라인 | 직추천자 3명 → 3개 독립 라인, 각각 다른 깊이 도달 | 각 라인을 독립적으로 계산 후 합산 | "라인당 최대 5%"이지 "전체 조직 매출의 5%"가 아님을 검증 |
| 자격 미충족 회원 포함 | 라인 중간에 당월 5만원 미달 회원 존재 | 그 회원의 구매 매출은 0원으로 라인 매출에 반영, 라인 깊이 판정 자체에는 영향 없음 | §3.5.2/§3.5.4 자격과 트리 구조 판정의 분리 |

### 2.2 정산(Settlement) 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | `settlement_items`/`commission_records` 등 append-only 원장에 대한 UPDATE/DELETE 직접 수정 금지 강제, 보정 엔트리(역분개) 방식 정정의 정확성, 정산 프로세스 단계 전이(§9 ①~⑦) |
| **왜** | [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md) §1.2가 "append-only 원장 테이블(`commission_records`, `settlement_items`, `audit_logs`)... 기존 행을 UPDATE/DELETE로 직접 수정하지 않는다. 정정은 반드시 보정(역분개) 엔트리 추가로 처리한다"고 명시하며, [SETTLEMENT-RULES.md](SETTLEMENT-RULES.md) §8이 "`settlement_items`는 append-only다... 정정은 반드시 보정 엔트리(역분개)로 처리한다... 모든 정정은 사유와 승인자를 기록한다"고 동일 원칙을 정산 도메인에서 재확인한다. 이는 법적·금전적 영향이 큰 영역([DO-NOT-TOUCH.md](DO-NOT-TOUCH.md) §1.2 "정산(커미션) 계산 로직")이므로 원장 불변성이 코드 리뷰가 아니라 **테스트로 강제**되어야 한다 |
| **어떻게(레벨)** | **Integration**: 실제 DB(또는 동등한 테스트 DB)에 대해 `settlement_items`/`commission_records` 행에 UPDATE/DELETE를 시도하는 테스트를 작성하고, 애플리케이션 레벨(repository/service 계층)에 UPDATE/DELETE 메서드 자체가 노출되지 않거나 호출 시 거부되는지 확인 — DB 권한(REVOKE UPDATE/DELETE) 또는 트리거로 강제하는 방식을 택할 경우 그 DB 제약 자체를 Integration 테스트로 검증. **Unit**: 보정 엔트리 생성 로직 — 정정 대상 금액의 음수 보정 엔트리가 원본과 동일한 스키마로 생성되는지, 사유/승인자 필드가 누락 없이 채워지는지. **Integration**: 정정 후 "원본 + 보정 엔트리"를 합산한 값이 올바른 정정 결과와 일치하는지(원본을 지우지 않고 합산으로 정합성을 회복하는 구조이므로) |

### 2.3 35% 법적 한도 게이트 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | 정상(30% 미만)/주의(30%↑)/경고(33%↑)/차단(35%↑) 3단계 임계치 동작, 차단 시 "③ 법적 한도 검증 → ④ 세금 계산" 전이 보류(Hard Gate), 연도 누적(Postgres) 값만이 법적 판단 근거이고 실시간 캐시(Redis)는 추정치로만 쓰이는지, 모니터링 대시보드 수치와 실제 정산 배치 처리 결과의 정합성 |
| **왜** | [ARCHITECTURE.md](ARCHITECTURE.md) §8.1은 "법적 차단은 항상 연도 누적(Postgres) 값만을 근거로 판단하며, 실시간 캐시는 대시보드 표시용 추정치로만 쓴다"고 명시하고, §8.1.3은 "차단"이 "정산 배치를 영구 폐기하는 것이 아니라... ③→④ 전이를 보류하는 Hard Gate"라고 정의한다. 이 구분이 깨지면(예: 캐시값으로 차단을 판단) 법적 한도를 실제로는 초과한 채 지급이 진행될 위험이 있다 — [COMPENSATION-RULES.md](COMPENSATION-RULES.md) §1의 "사업연도 매출액의 35%를 초과할 수 없다"는 법령상 hard constraint이므로 이 게이트의 오작동은 가장 심각한 등급의 결함이다 |
| **어떻게(레벨)** | **Unit**: 30%/33%/35% 각 경계값(boundary) 케이스 — 29.99%/30.00%/32.99%/33.00%/34.99%/35.00% 등 경계 바로 아래/위 비율 입력에 대해 정확한 단계(정상/주의/경고/차단)가 반환되는지. **Unit**: 분자/분모 정의가 [COMPENSATION-RULES.md](COMPENSATION-RULES.md) §6의 보수적 기본값(모든 패키지의 제품 판매수익·페어보너스를 한도 산정에 포함)과 일치하는지 — O-059 분류 결과가 바뀌어 분자 정의가 패키지별로 달라지는 시나리오도 케이스로 준비해 둔다. **Integration**: 캐시(Redis 실시간 카운터)와 Postgres(`compliance_ratio_snapshots`) 값에 의도적으로 drift를 주입한 뒤, 차단 게이트가 캐시값이 아니라 Postgres 연도 누적값으로 판단하는지 확인 — 이것이 "모니터링↔실제 정산 정합성"의 핵심 검증 지점. **Integration**: 35% 이상 시나리오에서 `settlement_batches`가 실제로 "검증" 단계에 머무르고 ④(세금 계산)/⑤(운영자 승인)로 자동 전이하지 않는지, 지정 관리자(SuperAdmin/Compliance Admin) 알림이 발생하는지(33% 경고 단계 포함). **Integration**: 개별 회원 단위 보류([SETTLEMENT-RULES.md](SETTLEMENT-RULES.md) §6)와 배치 단위 한도 차단(§8.1.3)이 동시에 걸리는 케이스 — 개별 보류된 항목이 한도 계산의 분자에서 제외되는지(ARCHITECTURE.md §8.1.3이 "구현 시 주의"로 명시한 지점) |

#### 임계치 경계값 테스트 매트릭스

| 비율 | 기대 단계 | 기대 동작 |
|---|---|---|
| 29.99% | 정상 | 표시만 |
| 30.00% | 주의 | 대시보드 강조 |
| 32.99% | 주의 | 대시보드 강조 |
| 33.00% | 경고 | 강조 + 관리자 알림 |
| 34.99% | 경고 | 강조 + 관리자 알림 |
| 35.00% | 차단 | ③→④ 전이 보류, 검토 요청 |
| 40.00%(초과) | 차단 | 동일(차단은 35% 이상 전부 동일 처리) |

### 2.4 쇼핑몰/주문 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | 주문 생성→재고 차감→배송 트리거 흐름, 패키지 구매 플로우(구매 확정 시점에 이벤트 기반으로 제품 판매수익/페어보너스 즉시 산정), 환불/취소 시 재고 환수 및 클로백 연계 |
| **왜** | [ARCHITECTURE.md](ARCHITECTURE.md) §2.2는 Order 모듈이 "주문 생성/취소 요청 접수, 즉시 반영 가능한 매출 기록"을 담당한다고 정의하고, [COMPENSATION-RULES.md](COMPENSATION-RULES.md) §4.1.2는 패키지 구매수익이 "배치 주기를 기다리지 않고 패키지 구매 확정 시점에 이벤트 기반으로 즉시 산정"된다고 명시한다 — 일반 후원수당(월배치)과 다른 타이밍 모델이므로, 주문 흐름과 수당 산정 트리거가 정확히 맞물리는지가 핵심 위험 지점이다 |
| **어떻게(레벨)** | **Unit**: 재고 차감 계산, 패키지 정책(추천수당 비율/금액, 페어 활성화 여부)에 따른 산정 분기. **Integration**: 주문 생성 API(`api`) 호출 → Job 생성 → worker 처리 → `orders`/`inventory` 갱신까지의 흐름, 패키지 구매 확정 이벤트가 실제로 `package_sales_profits`/`package_pair_bonuses` 생성을 즉시 트리거하는지(월배치를 기다리지 않는지). **E2E**: 회원가입→패키지 구매→추천인에게 제품 판매수익 지급까지의 critical path 1~2개 |

### 2.5 Workflow Engine 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | 신규 워크플로우(환불/반품/교환/전자결재 등)의 승인 단계 전이(순차 다단계, 자동승인 조건, 알림 연동), **그리고 기존 5개 전용 승인구조가 Workflow Engine으로 흡수되지 않았음을 검증하는 회귀 테스트** |
| **왜** | [PRD.md](PRD.md) §5.30.3은 조직 이동 승인(D-020/D-022)·회원 생애주기 변경 승인(D-006)·프로그램 신청 승인(D-042)·포인트 사용신청 승인(D-041)·정산 운영자 승인([SETTLEMENT-RULES.md](SETTLEMENT-RULES.md) §9)의 5개를 "유지 — 변경하지 않음"으로 명시하고, [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md) §1.1은 "ERP Core 엔진(Workflow Engine 등, D-046)을 도입한다고 해서 기존 전용 구조를... 임의로 그 엔진으로 재구현하지 않는다"고 못박는다. 이는 "기능이 동작하는지"보다 "**누군가 실수로 통합하지 않았는지**"를 지키는 회귀 테스트가 핵심이라는 의미다 — 일반적인 정상 동작 테스트와 반대로, 통합이 발생하면 실패해야 하는 negative 테스트 성격이 강하다 |
| **어떻게(레벨)** | **Unit**: Workflow Engine 자체의 단계 전이 로직(순차 승인, 자동승인 조건 평가, 분기 조건) — 신규 워크플로우 기준. **Integration**: 5개 전용 구조 각각이 **여전히 독립된 테이블/엔드포인트를 사용**하는지의 구조적 검증 — 예를 들어 조직 이동 승인 호출이 `organization_transfer_logs`를 경유하고 Workflow Engine의 공용 테이블(workflow_instances 등)을 전혀 거치지 않는지, 조직 이동의 "9개 사유코드·3개 역할 제한"이라는 전용 role guard([ARCHITECTURE.md](ARCHITECTURE.md) §4)가 Workflow Engine의 범용 역할 지정 방식으로 대체되지 않았는지. **Integration**: 정산 운영자 승인(§9 ⑤단계)이 Workflow Engine을 호출하지 않고 기존 경로로만 처리되는지 — 이 회귀 테스트는 Workflow Engine 관련 코드가 변경될 때마다(다른 워크플로우 기능 추가 시에도) CI에서 상시 실행해, "5개 전용 구조 비흡수" 원칙이 미래의 무관한 변경에 의해 우연히 깨지지 않도록 가드레일 역할을 하게 한다 |

### 2.6 API 테스트 — "api는 계산하지 않는다" 원칙 자체의 검증

| 항목 | 내용 |
|---|---|
| **무엇을** | api(NestJS) request handler가 무거운 계산(후원수당/정산/세금/프로모션 판정/보고서 생성)을 직접 수행하지 않고 Job 생성만 하는지 — 이 아키텍처 원칙 자체를 테스트로 신호화 |
| **왜** | [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md) §1.3은 "api(NestJS)의 request handler에 무거운 계산... 을 직접 구현하지 않는다. api는 Job 생성과 상태/결과 조회만 담당한다"고 명시하고, [ARCHITECTURE.md](ARCHITECTURE.md) §1.1/§3도 동일하게 "api request handler는 무거운 계산을 하지 않는다 (확정 — 위반 금지)"고 반복한다. 이 원칙은 코드 리뷰만으로는 시간이 지나며 서서히 깨지기 쉬운(누군가 "임시로" 계산 로직을 api에 넣는) 종류의 위반이므로, **응답 시간을 신호(proxy metric)로 삼는 자동화된 가드**가 필요하다 |
| **어떻게(레벨)** | **Integration**: 정산/수당/세금/프로모션/보고서 생성 관련 모든 api 엔드포인트에 대해 응답이 **202 Accepted + Job id** 패턴을 따르는지 구조적으로 확인([ARCHITECTURE.md](ARCHITECTURE.md) §3) — 200 OK로 계산 결과를 직접 반환하면 그 자체로 실패. **Integration(가드레일, 성능 신호)**: api 응답시간이 일정 임계치(예: p95 200ms~500ms 등 — 정확한 값은 구현 단계에서 운영 데이터로 보정 필요, 본 문서는 "신호로 삼는다"는 원칙만 제시하고 정확한 임계값은 미정 사항으로 남김)를 초과하는 엔드포인트가 발견되면 "계산이 새어들어갔다"는 가설을 세우고 코드 리뷰를 트리거하는 모니터링/알림을 CI 또는 운영 환경에 둔다 — 응답시간 자체가 버그를 증명하지는 않지만(네트워크/DB 지연 등 다른 원인도 있음), Job 생성만 하는 엔드포인트는 본질적으로 빨라야 하므로 이상 신호로는 유효하다. **Unit**: api 모듈(Compensation/Settlement/Compliance 등)의 컨트롤러·서비스 코드에 대해 정적 분석(예: 특정 계산 함수/라이브러리 import 금지 룰)을 두어, worker 전용으로 분리된 계산 함수가 api 패키지에서 import되면 빌드 단계에서 실패하게 하는 린트 규칙 — 이는 테스트라기보다 빌드 가드이지만 동일한 목적(api 계산 금지)을 더 이른 단계에서 강제 |

### 2.7 Idempotency 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | Job 재시도(Redis Retry) 시 동일 커미션/동일 알림이 중복 생성되지 않는지 |
| **왜** | [DATABASE.md](DATABASE.md) §7.6 운영 위험이 "Redis Retry로 재시도되는 Job(수당 계산, 알림 발송 등)이 중복 실행될 경우, 중복 커미션 지급이나 중복 알림 발송이 발생할 위험"을 명시하고, `idempotency_key` 도입을 §7.2/§8에서 제안한다(Open Decision O-054, [DECISIONS.md](DECISIONS.md)). 금전이 직접 걸린 영역(중복 커미션 지급)이므로 멱등성 결함은 회귀위험이 가장 높은 카테고리 중 하나다 — O-054가 아직 "도입 범위 미확정"이라는 점은 설계 공백이 있다는 의미이므로, 테스트는 구현 단계 진입 시 이 공백이 메워졌는지를 확인하는 역할도 겸한다 |
| **어떻게(레벨)** | **Unit**: 동일 `idempotency_key`로 두 번 호출된 계산 함수/서비스 메서드가 두 번째 호출에서 새 행을 생성하지 않고 기존 결과를 그대로 반환(또는 no-op)하는지. **Integration**: 동일 Job을 의도적으로 두 번(또는 N번) worker에 재투입(실제 Redis Retry 상황을 시뮬레이션)했을 때 `commission_records`/`notification_logs`에 중복 행이 생기지 않는지 — Job 페이로드에 멱등키가 없는 구버전 호환 케이스도 함께 점검(있다면). **Integration**: 알림 발송(Notification Center, worker)의 재시도 시나리오 — 동일 알림이 회원에게 두 번 발송되지 않는지(Email/SMS/KakaoTalk/Push 채널별로 각각 확인, [ARCHITECTURE.md](ARCHITECTURE.md) §2.3). **주의**: O-054(`idempotency_key` 도입 범위)가 미확정이므로, 본 절의 테스트 케이스는 "이 키가 도입된 이후" 전제의 명세다 — 구현 착수 시 O-054가 먼저 확정되어야 본 절의 테스트가 실질적으로 작성 가능하다 |

### 2.8 ERP Core 엔진 테스트 (Workflow/API Center/File Manager/Scheduler/Notification/Audit/Dashboard·Report·Form Builder/System Settings)

| 항목 | 내용 |
|---|---|
| **무엇을** | ERP Core 12개 엔진(Workflow Engine/API Center/File Manager/Scheduler Center/Notification Center/Audit Center/Dashboard·Report·Form Builder/System Settings)이 **업무 모듈(쇼핑몰/MLM/CMS)에 의존하지 않는다**는 D-046 원칙의 검증, 그리고 §2.5에서 다룬 "기존 5개 전용 승인구조 비흡수" 외의 나머지 엔진별 핵심 동작 |
| **왜** | [PROJECT-CONTEXT.md](PROJECT-CONTEXT.md) §1.1 "ERP Core는 업무 모듈에 의존하지 않는다(D-046, 확정 원칙)"는 향후 업무 모듈을 추가/제거해도 ERP Core가 동작해야 한다는 아키텍처 보증이다 — 이 의존 방향이 거꾸로(ERP Core → 업무 모듈) 새지 않았는지는 코드 리뷰만으로 놓치기 쉬워 테스트로 고정할 필요가 있다 |
| **어떻게(레벨)** | **Integration**: 업무 모듈 없이(목업 `subject_type`/`subject_id`만으로) Workflow Engine 인스턴스를 생성·승인·반려할 수 있는지(D-046 의존성 역전 방지). **Integration**: API Center의 외부 연동(`external_api_connections`) CRUD가 PG/3PL/SMS 등 특정 업무 모듈 코드를 import하지 않고 동작하는지. **Unit**: Scheduler Center가 트리거하는 cron이 실제 계산을 포함하지 않고 Job 생성만 하는지(§2.6과 동일한 "계산 분리" 원칙을 scheduler에도 적용). **Integration**: System Settings 허브가 기존 분산 설정(§5.27)을 중복 저장하지 않고 참조만 하는지(`tenant_settings`/`system_security_policies` 등) |

### 2.9 Multi-Tenant 격리 테스트

| 항목 | 내용 |
|---|---|
| **무엇을** | 한 테넌트의 요청/Job/데이터가 다른 테넌트의 데이터에 접근하거나 영향을 주지 않는지 |
| **왜** | [DECISIONS.md](DECISIONS.md) O-159(Multi-Tenant 활성화 시 Job 격리)·O-170(온보딩/모니터링/격리검증)이 아직 미확정이지만, "구조 준비, 활성화는 보류"(D-035) 원칙상 구조 자체는 이미 존재(`tenants`/`tenant_settings`, [DATABASE.md](DATABASE.md) §3.31) — 활성화 이전에도 데이터 격리 테스트 전략을 미리 정의해 두면 활성화 시점에 즉시 검증 가능하다 |
| **어떻게(레벨)** | **Integration**: 테넌트 A의 토큰으로 테넌트 B의 리소스 ID를 직접 호출했을 때 403/404로 거부되는지(RLS 적용 여부와 무관하게 애플리케이션 레벨에서도 검증, O-020 RLS 범위 미확정과는 별개 방어선). **Integration**: 한 테넌트의 대량 배치(Bulk Action/대량 수당계산)가 다른 테넌트의 Job 큐 처리 지연을 유발하지 않는지(O-159 확정 후 구체화 가능 — 확정 전에는 "현재 단일 큐이므로 격리 보장 없음"을 명시적으로 아는 상태로 둔다). **주의**: O-159/O-170이 미확정이므로 본 절도 §2.7과 동일하게 "확정 후 구체화"가 필요한 명세 수준이다 |

### 2.10 쇼핑몰 운영 Phase 2 / SEO / Digital Marketing 테스트 (D-070 신규)

| 항목 | 내용 |
|---|---|
| **무엇을** | `product_seo`(DATABASE.md §3.55) 자동생성 규칙(BR-053)의 우선순위 처리, Schema.org 가격/재고/리뷰평점의 실시간 파생 여부, sitemap.xml/robots.txt의 쿼리타임 생성(BR-054), 카카오/SNS 공유 미리보기 및 Google/Naver Preview의 렌더링 정확성, OG 메타태그 정확성, 상품 Feed(Google Shopping/Facebook/Naver Shopping) 생성·자동갱신, Digital Marketing 연동(`external_api_connections`, PRD.md §5.47) 등록/상태 관리, SEO Dashboard(§5.48) 지표 표시 |
| **왜** | D-069/D-070(쇼핑몰 운영 고도화 및 Phase 2)에서 추가된 기능은 모두 신규 Business Rule(BR-053/BR-054)이거나 기존 ERP Core 패턴(Dashboard Builder/Scheduler Center/External API Center) 재사용이다 — 회귀 위험은 "자동생성값과 관리자 수동값의 우선순위가 뒤바뀌는 것"(BR-053 위반)과 "캐시된 값이 즉시 반영되지 않는 것"(Schema.org 실시간 파생 원칙 위반)에 집중된다. [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md)가 강조하는 정산/MLM 계산만큼 금전적으로 치명적이지는 않지만, 검색엔진 노출·소셜 공유 품질에 직접 영향을 주므로 별도 영역으로 추적할 필요가 있다 |
| **어떻게(레벨)** | **SEO 테스트(Unit)**: 관리자가 `product_seo` 필드를 직접 입력하지 않은 경우 상품명→Title/요약설명→Description/대표이미지→OG Image 자동매핑이 정확한지. **SEO 테스트(Integration)**: 관리자가 수동으로 값을 입력한 후에는 그 값이 항상 우선하고 자동생성 로직이 덮어쓰지 않는지(BR-053 핵심 회귀 포인트). **Schema 테스트(Integration)**: 가격/재고/리뷰평점 변경 직후 Schema.org 출력이 즉시 새 값을 반영하는지(저장된 캐시값이 노출되면 실패) — 저장 컬럼이 아니라 매 조회 시 `products`/`product_option_combinations`/리뷰 집계에서 파생되는지 확인. **자동 Sitemap 테스트(Integration)**: 신규 상품 등록/비공개 전환 직후 sitemap.xml에 즉시 반영(추가/제외)되는지, robots.txt가 별도 원장 없이 매 요청 시 동적 생성되는지(BR-054). **OG 테스트(Unit)**: og:title/og:description/og:image이 자동생성값 또는 관리자 입력값 중 BR-053 우선순위대로 정확히 채워지는지. **카카오 공유 테스트 / Google Preview 테스트(E2E, 프론트엔드 렌더링 레벨)**: `tenant_share_images`(§3.55) 업로드 값과 `product_seo` 입력값이 카카오톡/Google/Naver/Facebook/Twitter 미리보기 레이아웃에 정확히 반영되는지 — §5.46.4가 "순수 프론트엔드 렌더링, DB 영향 없음"이라고 명시하므로, 이 테스트는 렌더링 레벨에 한정하고 데이터 저장 레벨 검증과 명확히 분리한다. **Feed 테스트(Unit)**: Google Shopping/Facebook/Naver Shopping 포맷별 스펙 차이(필수 필드/형식)가 정확히 반영되는지. **Feed 테스트(Integration)**: Feed 생성 시 상품/SEO/가격/재고 데이터가 정확히 조합되는지, 자동 갱신 Job이 Scheduler Center 패턴(§2.8과 동일 원칙)대로 동작하는지. **Analytics 설정 테스트(Integration)**: `external_api_connections`에 GA4/GTM/Meta Pixel 등 category 값을 등록했을 때 연동 상태(TESTING/ACTIVE/INACTIVE)가 올바르게 전이하는지 — **실제 GA4/Meta 등으로의 데이터 전송 자체는 본 라운드 설계 범위가 아니므로(PRD.md §5.47, "실제 연동 구현 없음") 이 테스트는 연동 등록/상태 관리 레벨에 한정된다.** **SEO Dashboard 지표 테스트**: O-195(검색엔진 지표를 자체 캐시할지 실시간 API 호출할지)가 미확정이므로, §2.7/§2.9와 동일하게 **본 항목은 O-195 확정 후에야 구체화 가능한 명세 수준**으로 남긴다 |

### 2.11 쇼핑몰 UX/알림/운영자 대시보드 테스트 (D-072 신규)

| 항목 | 내용 |
|---|---|
| **무엇을** | 장바구니(`carts`/`cart_items`, DATABASE.md §3.57 신규) CRUD와 조회 시점 파생 표시(품절/가격변경/배송비예상/쿠폰적용가능여부), 관심상품(`product_wishlists`, 기존)·최근 본 상품(`recently_viewed_products`, 기존)·상품 비교(`product_comparisons`, §3.54) 조회/관리, 가격인하 알림(`product_price_alerts`, 신규) 등록·발송, 결제 실패 재시도(`order_payment_attempts`, §3.53)와 STATE-MACHINE.md §18 정합성, 배송 추적(`shipments` 컬럼 명료화)과 §17 정합성, 반품/교환/환불 플로우(§16)의 청약철회권 기한 적용, 자동결제 실패 알림(§19)과 §5.56 이벤트 카탈로그 연동, 알림 템플릿(`notification_templates.subject_template`/`is_active`, 신규 컬럼) 동작과 다국어 처리, 알림 발송 실패/재발송(§20), 운영자 대시보드 지표(PRD.md §5.59) 정확성, 관리자 업무 Queue(기존 테이블 횡단 조회) 동기화 |
| **왜** | DATABASE.md §3.57은 "가능한 모든 곳에서 기존 테이블을 재사용했다 — 신규 테이블은 `carts`/`cart_items`와 `product_price_alerts` 2종뿐"이라고 명시한다 — 즉 본 라운드의 회귀 위험은 신규 테이블 자체보다 **기존 테이블 재사용 지점에서 조회 시점 파생값이 저장값처럼 굳어버리는 것**(§2.10의 BR-053/Schema.org 실시간 파생 원칙과 동일 계열의 위험)과 **STATE-MACHINE.md §17~§20(이번 라운드 신규 상태 다이어그램)이 실제 테이블 상태 전이와 어긋나는 것**에 집중된다. 또한 PRD.md §5.58.2/§5.59가 "장바구니 전환율은 `carts` 도입으로 비로소 측정 가능해진 지표"라고 명시하므로, 신규 테이블 하나가 기존 대시보드 지표의 정확성에 새로 영향을 주는 흔치 않은 케이스다 |
| **어떻게(레벨)** | **장바구니 UX(Unit)**: `cart_items` 추가/수량변경/옵션변경/선택삭제 각 연산이 정확한 행을 정확히 갱신/삭제하는지. **장바구니 UX(Integration)**: 품절/가격변경/배송비예상/쿠폰적용가능여부가 `cart_items`에 저장된 값이 아니라 매 조회 시 `product_option_combinations`/`shipping_fee_policies`/`coupons`(DATABASE.md §3.57)와 조합한 파생값인지 — 상품 가격이나 재고가 바뀐 직후(장바구니에 담아둔 채로) 장바구니 화면을 새로고침하면 즉시 새 값이 반영되는지가 핵심 회귀 포인트(캐시된 옛값이 보이면 실패). **관심상품(Unit/Integration)**: `product_wishlists` CRUD. 장바구니→관심상품 이동("나중에 구매하기")이 `cart_items`를 삭제하는지/상태 플래그로 보존하는지는 **O-197 미확정**이므로 본 항목은 §2.7/§2.9/§2.10과 동일하게 O-197 확정 후에야 구체화 가능한 명세 수준으로 남긴다. **최근 본 상품(Unit)**: `recently_viewed_products` 기록(조회 시 자동 적재)·조회(최신순) 정확성. **상품 비교(Unit/Integration)**: `product_comparisons` 추가/삭제/목록조회, 비회원 처리(서버 저장 vs 클라이언트 저장)는 **O-188 미확정**이므로 비회원 케이스는 확정 후 구체화. **가격인하 알림(명세 수준)**: `product_price_alerts` 등록/발송 자체는 테스트 가능하나, 트리거 기준(절대가 vs 할인율)이 **O-198 미확정**이므로 경계값 테스트는 O-198 확정 후 작성. **결제 실패 재시도(Unit)**: `order_payment_attempts` 상태 전이가 STATE-MACHINE.md §18(ATTEMPTED→SUCCEEDED/FAILED→RETRY_REQUESTED, 이번 라운드 신규)의 경계와 정확히 일치하는지(예: FAILED가 아닌 상태에서 RETRY_REQUESTED로 전이 시도 시 거부되는지). **결제 실패 재시도(Integration)**: 재시도 발생 시 §5.56 알림 이벤트 카탈로그의 "결제실패"/"재결제요청" 이벤트가 정확히 트리거되는지 — §2.10이 다룬 Notification 발송 패턴과 동일 원칙 재사용. **배송 추적(Unit)**: `shipments.courier_name`/`tracking_no`/`status`가 STATE-MACHINE.md §17(PREPARING→DISPATCHED→IN_TRANSIT→DELAYED/HOLD→DELIVERED, 이번 라운드 신규)의 허용 전이 경계값(예: DELIVERED에서 역행 전이 시도 시 거부)을 정확히 지키는지. **배송 추적(Integration)**: 송장번호(`tracking_no`) 등록/상태 변경 시 `shipment_change_logs`(§3.53)에 이력이 빠짐없이 기록되는지. **반품/교환/환불(Integration)**: STATE-MACHINE.md §16(REQUESTED→INSPECTING→APPROVED/REJECTED→REFUNDING/RESHIPPING→COMPLETED, 기존 D-069)의 각 단계 전이에서 청약철회권 기한([LEGAL-CHECKLIST.md](LEGAL-CHECKLIST.md) §4)이 정확히 적용되어 취소/반품 가능 여부 판정이 갈리는지, 환불 예상 금액 표시가 `orders.status`/정책을 조합한 파생값이지 별도 저장값이 아닌지 — **LEGAL-CHECKLIST.md §4 자체가 청약철회 가능 기간·조건을 "원문 확인 필요"로 미확정 상태로 남기고 있으므로, 정확한 기간 경계값 테스트는 그 법률 확인 이후에야 완성될 수 있다는 점을 명세에 남긴다.** **자동결제 실패 알림(Integration)**: STATE-MACHINE.md §19(SCHEDULED→SUCCEEDED/FAILED→RETRYING→PAUSED/CANCELLED, 이번 라운드 신규)의 전이마다 §5.56의 해당 이벤트(자동결제실패/자동결제재시도/정기배송일시정지/정기배송해지)가 정확히 발송되는지 — 재시도 횟수·간격·연속 실패 처리 자체는 기존 **O-086(정기배송 자동결제 실패/재시도 정책 미확정)**에 종속되므로, 본 항목의 전이-알림 매핑 검증은 가능하지만 재시도 횟수/간격의 정확한 경계값 테스트는 O-086 확정 후로 미룬다. **알림 템플릿(Unit)**: `notification_templates.subject_template`/`is_active`(신규 컬럼) 토글 동작, 변수({{member_name}}/{{order_no}}/{{tracking_no}} 등)가 `content_template`/`subject_template` 내 placeholder로 정확히 치환되는지. **알림 템플릿(Integration)**: `cms_translations`(§3.33, `content_type='notification_templates'` 기존 패턴) 다국어 템플릿이 수신 회원의 locale에 맞게 정확히 선택되는지, 미리보기(렌더링만, DB·발송 side-effect 없음)와 테스트발송(임의 수신자 1회성 실제 발송, 운영 발송과 동일 경로 사용)이 서로 명확히 구분되어 테스트발송이 실수로 운영 로그(`notification_logs`)를 오염시키지 않으면서도 발송 경로 자체는 동일한지. **알림 발송 실패(Integration)**: STATE-MACHINE.md §20(QUEUED→SENDING→SENT/FAILED→RESENT, 이번 라운드 신규) 전이 — 동일 알림이 재발송(RESENT) 시 중복 발송되지 않는지(§2.7 Idempotency 테스트와 동일 원칙 재사용), `resent_from_log_id`(`notification_logs`, §3.41)로 원본 로그가 정확히 추적되는지. **운영자 대시보드 지표(Unit)**: "오늘 지표"(PRD.md §5.59)가 정확한 당일(자정 기준) 범위로 집계되는지 — 국가별 타임존 차이 처리는 미확정이므로, 단일 타임존 가정 하의 경계값(자정 직전/직후 트랜잭션의 당일 포함 여부)만 우선 테스트하고 다국가 타임존 케이스는 추후 Open Decision 등록 후 확장. **운영자 대시보드 지표(Integration)**: "쇼핑몰 운영 지표"의 장바구니 전환율이 `carts`/`cart_items` 도입 이후 비로소 측정 가능해진 지표(PRD.md §5.59)이므로, 그 계산식(장바구니담기 수÷상품조회 수 등)이 동일 대시보드의 "주문/결제 전환율" 등 기존 전환율 지표와 분모/분자 정의 방식이 일관되는지 — 정확한 공식 자체는 현재 문서에 명문화되어 있지 않으므로, 본 테스트는 "공식이 확정되면 그 공식이 다른 전환율 지표와 동일한 정의 패턴(분모: 조회/노출 수, 분자: 전환 행위 수)을 따르는지"를 확인하는 명세 수준으로 남긴다. **관리자 업무 Queue(Integration)**: Task Queue(PRD.md §5.60)가 신규 테이블이 아니라 `workflow_instances`/`order_payment_attempts`/`exchange_requests`/`return_items`/`product_reviews`/재고/CS 티켓 등 기존 테이블을 횡단 조회하는 파생 뷰(DATABASE.md §3.57)이므로, 각 항목이 원본 테이블에서 처리 완료 상태로 바뀐 직후(예: 반품 승인) Queue 목록에서 누락 없이 즉시 사라지는지 — Queue가 별도 상태를 갖지 않고 원본 테이블 상태만 비추는 미러 구조이기 때문에 발생할 수 있는 동기화 지연/누락 회귀가 핵심 검증 지점이다 |

### 2.12 운영 UX 및 고객 경험 테스트 (D-073 신규)

| 항목 | 내용 |
|---|---|
| **무엇을** | Abandoned Cart 감지(Scheduler Job, PRD.md §5.62)의 임계 시간 판정과 완료 주문 제외, Abandoned Cart 알림의 중복 발송 방지(`notification_logs` 조회 기반), Abandoned Cart 자동 쿠폰 발급(`coupon_issuances`)의 정확성·중복방지, Customer Timeline(PRD.md §5.64) federated 조회의 정합성·정렬, My Dashboard(PRD.md §5.65)의 관리자 간 격리(`owner_admin_id`/`is_shared`), §5.59 신규 위젯(SEO 오류/Feed 오류) 카운트 정확성, Quick Action(§5.61)이 기존 승인 워크플로우를 우회하지 않는지(회귀) |
| **왜** | PRD.md §5.62~§5.66은 모두 "신규 테이블 없음 — 기존 구조 재사용"을 전제로 설계되었다(D-073 hard constraint, 신규 DB 테이블/컬럼 0건). 따라서 본 라운드의 회귀 위험은 새로운 데이터 모델이 아니라 **기존 테이블을 조합하는 로직 자체의 정확성**에 집중된다 — Abandoned Cart는 "장바구니 idle 판정 + 중복 발송 방지 + 쿠폰 중복 발급 방지"라는 3중 로직이 모두 기존 테이블(`cart_items`/`notification_logs`/`coupon_issuances`) 조회만으로 정확히 동작해야 하고(별도 "처리됨" 플래그 컬럼이 없으므로 §2.7 Idempotency 테스트와 동일한 "매번 로그 조회로 판단" 원칙이 핵심), Customer Timeline은 §2.10/§2.11이 이미 확립한 "저장값이 아니라 조회 시점 federated 파생값" 원칙(BR-053/Schema.org 실시간 파생과 동일 계열)을 9개 원본 테이블 횡단으로 확장한 것이다. My Dashboard는 D-072(§5.59/§5.60)가 이미 검증한 Dashboard Builder/Widget 구조에 "소유자 스코프" 한 축만 추가되므로 회귀 위험은 격리 누수(다른 관리자 위젯이 보이는 것) 한 지점에 집중된다. Quick Action 무회피 테스트는 §2.5(Workflow Engine 비흡수 회귀)와 동일한 "지름길이 기존 승인 절차를 우회해서는 안 된다"는 negative 테스트 성격이다 |
| **어떻게(레벨)** | **Abandoned Cart 감지(Unit)**: Scheduler Job의 임계 시간 경계값 — 23시간59분 경과(미감지) vs 24시간00분 경과(감지)의 정확한 분기(PRD.md §5.62, 정확한 기본값은 구현 단계 TBD이므로 경계값 자체는 설정 가능한 임계 변수로 두고 그 변수 기준의 경계 동작을 테스트). **Abandoned Cart 감지(Integration)**: idle 조건을 만족하더라도 동일 회원의 해당 장바구니로 이미 완료된 주문(`orders`)이 존재하면 감지 대상에서 제외되는지. **Abandoned Cart 중복 알림 방지(Integration)**: 동일 회원에게 짧은 시간 내 `ABANDONED_CART_REMINDER` 알림이 중복 발송되지 않는지 — 별도 "발송됨" 플래그 컬럼이 없으므로 매번 `notification_logs`를 조회해 최근 발송 여부를 판단하는 로직이 정확한지가 핵심 회귀 포인트(§2.7 Idempotency 테스트와 동일 원칙 재사용). **Abandoned Cart 자동 쿠폰(Integration)**: 쿠폰 발급이 `coupon_issuances`에 정확히 기록되는지, 동일 회원에게 동일 Abandoned Cart 이벤트로 중복 발급되지 않는지(§2.7/§2.9/§2.10/§2.11이 이미 사용한 "로그 조회 기반 중복 방지" 패턴 재사용) — **발급 트리거 소유 모듈(Scheduler Center vs 쇼핑몰 모듈)이 O-192 미확정**이므로, 본 항목은 §2.7/§2.9/§2.10/§2.11과 동일하게 O-192 확정 후에야 구체화 가능한 명세 수준으로 남긴다. **Customer Timeline 정합성(Integration)**: `members`/`orders`/`order_payment_attempts`/`shipments`/CS 티켓/`returns`/`exchange_requests`/`settlement_items`/`point_transactions`/`member_activity_logs`(activity_type=LOGIN)/`notifications`/`notification_logs` 중 어느 원본 테이블에든 새 이벤트가 발생하면 Timeline 조회 시(저장된 캐시 없이) 즉시 반영되는지 — 캐시된 스냅샷이 노출되면 실패. **Customer Timeline 정렬(Unit)**: 원본 테이블마다 다른 타임스탬프 컬럼명(`created_at`/`occurred_at` 등)을 통일된 정렬 키로 매핑해 시간순으로 정확히 정렬하는지. **My Dashboard 격리(Integration)**: 관리자 A가 자신의 Dashboard에 추가한 위젯(`dashboard_widgets`)이 `owner_admin_id`=A, `is_shared`=false인 경우 관리자 B의 Dashboard 조회 시 노출되지 않는지. **My Dashboard 공유 전환(Integration)**: 동일 위젯을 `is_shared`=true로 전환하면 관리자 B의 Dashboard에도 노출되는지. **SEO 오류 위젯(Unit)**: `product_seo`(§3.55)의 빈 필드(title/description/OG-image) 중에서도 자동생성 fallback이 성공적으로 채운 경우는 오류로 집계되지 않고, fallback까지 실패한 경우만 정확히 집계되는지(BR-053 우선순위 규칙과의 일관성 확인 — §2.10의 BR-053 테스트와 동일 원칙 재사용). **Feed 오류 위젯(Integration)**: `scheduled_job_run_logs`(§3.40)에서 Feed-type Job 중 status=FAILED인 건수와 위젯 카운트가 정확히 일치하는지. **Quick Action 무회피 회귀(Integration)**: Quick Action(예: 환불승인)으로 진입한 작업이 일반 화면으로 진입했을 때와 동일한 승인 상태머신(`returns`/exchange 기존 승인 워크플로우)과 권한 검증을 거치는지 — Quick Action 경로가 권한 검증이나 승인 단계를 생략하는 지름길이 되지 않았는지 확인하는 negative 테스트(§2.5의 "Workflow Engine 비흡수 회귀 테스트"와 동일한 "우회 방지" 성격, CI에 상시 포함 권장) |

### 2.13 Dynamic Board Engine 테스트 (D-074 신규)

| 항목 | 내용 |
|---|---|
| **무엇을** | 게시판(`boards`) 생성 시 `board_type`이 자유 확장 분류값임을 검증(고정 enum 강제 아님), 게시판 비활성화(`is_active=false`)의 즉시 노출 차단과 데이터 보존, 게시판 삭제 시 하위 `board_posts`/`board_categories`/`board_post_comments`/`board_post_likes` 처리 방식, [ROLE-MATRIX.md](ROLE-MATRIX.md) §27 신규 권한(게시판 생성 제한 역할 vs 게시글 등록 권한 확장 가능 범위, 회원 본인 댓글/좋아요 한정)의 강제 여부, 첨부파일(File Manager `files`, `related_entity_type='board_posts'`) 연계와 다중 이미지 순서 보존, SEO(`content_seo_metadata`) 연결 및 BR-053 우선순위 재사용, 예약게시(`scheduled_publish_at` → `SCHEDULED`→`PUBLISHED`) 전이, 승인후게시(`feature_flags.승인후게시=true`)의 `PENDING_APPROVAL` 시작과 Workflow Engine(`subject_type='BOARD_POST_APPROVAL'`) 연동, 다국어(`cms_translations`, `content_type='board_posts'`) 오버레이·폴백, 그리고 **기존 CMS(`cms_pages`/`faq_categories`/`faq_items`/`popups`/`banners`) 무변경 회귀** |
| **왜** | [DATABASE.md](DATABASE.md) §3.58은 `board_type`을 "`marketing_programs.category`(§3.34)와 동일한 자유 확장 분류값(고정 enum 강제 아님)"이라고 명시한다 — 이는 D-074의 핵심 가치 제안("코드 배포 없이 새 게시판 유형 생성")이 실제로 동작하는지를 가르는 지점이므로 별도 검증이 필요하다. 또한 §3.58 서두와 [PRD.md](PRD.md) §5.67 상태줄은 "**기존 CMS는 변경하지 않는다**"는 본 라운드의 핵심 제약을 반복해서 강조하므로, 이는 §2.5/§2.8이 이미 확립한 "비흡수/무변경 회귀 테스트" 패턴을 그대로 적용해야 하는 영역이다. [ROLE-MATRIX.md](ROLE-MATRIX.md) §27은 "신규 권한 테이블을 만들지 않는다 — 기존 7역할×11액션 체계를 그대로 적용한다"([PRD.md](PRD.md) §5.67.7)면서도 `boards`/`board_posts`가 §9(CMS)와 별개 데이터 엔터티이므로 신규 §27 권한표를 신설했고, P5/P6에서 "회원(Partner)은 본인 댓글/좋아요만 수정·삭제 가능, 타인 것은 불가"를 명시한다 — 이 경계가 실제로 강제되는지는 회원 데이터 보호 관점에서 중요하다. 첨부파일/SEO/다국어/예약게시/승인후게시는 모두 [DATABASE.md](DATABASE.md) §3.58이 "기존 구조 재사용 매핑" 표로 명시한 지점이므로, 신규 로직을 새로 검증하는 것이 아니라 **기존에 이미 검증된 패턴(File Manager 다형 참조, BR-053, §3.33 다국어 폴백, products.publish_start_at 예약게시, Workflow Engine 범용 승인)이 새 엔터티(`board_posts`)에 연결될 때도 깨지지 않는지**를 확인하는 회귀 성격의 테스트가 중심이 된다. 게시판/게시글 삭제 시 하위 데이터 처리 방식은 [ROLE-MATRIX.md](ROLE-MATRIX.md) §27 각주1이 "소프트 삭제(`is_active=false`) 우선 원칙([DATABASE.md](DATABASE.md) §4 원칙 3)을 따를 개연성이 높다... 정확한 정책은 미확정"이라고 밝히는 지점이므로, 명세 수준 표시가 필요하다 |
| **어떻게(레벨)** | **게시판 생성/비활성화(Unit)**: 기존에 존재하지 않던 임의의 `board_type` 문자열(예: "CSR_REPORT")로 게시판 생성을 시도했을 때 거부되지 않고 정상 저장되는지 — 고정 enum 컬럼이었다면 실패할 입력값을 의도적으로 사용하는 negative-style 검증. **게시판 비활성화(Integration)**: `is_active=false` 전환 직후 쇼핑몰/회원몰/마이오피스/메인(`shop_exposure`/`shop_member_exposure`/`my_office_exposure`/`main_exposure`) 각 노출 채널에서 해당 게시판과 그 게시글 목록이 즉시 사라지는지(캐시된 옛 노출 상태가 보이면 실패). **게시판 비활성화 데이터 보존(Integration)**: 비활성화 후에도 `board_posts`/`board_categories`/`board_post_comments`/`board_post_likes` 행 자체는 물리적으로 삭제되지 않고 DB에 보존되는지(소프트 삭제 원칙, [DATABASE.md](DATABASE.md) §4 원칙 3) — 재활성화(`is_active=true`) 시 기존 게시글이 그대로 복원되는지 함께 확인. **게시판 삭제(명세 수준)**: 게시판 삭제 시 하위 `board_posts`/`board_categories`/`board_post_comments`/`board_post_likes`가 cascade 물리 삭제되는지 소프트 삭제로 보존되는지의 정확한 정책은 [PRD.md](PRD.md)/[DATABASE.md](DATABASE.md)에 명시되어 있지 않다([ROLE-MATRIX.md](ROLE-MATRIX.md) §27 각주1 "정확한 정책은 미확정") — **본 항목은 구현 단계에서 정책 확정 필요로 명세 수준에 남긴다.** 다만 기존 O-117(Undo 복구 가능 시간/소프트 삭제→영구 삭제 전환 시점)이 이미 다루는 소프트 삭제 패턴과 일관되어야 한다는 원칙만 사전에 못박아 둔다 — cascade 물리 삭제로 결정될 경우 O-117의 소프트 삭제 우선 원칙과 충돌하지 않는지 구현 착수 시 재확인이 필요하다. **게시판 권한(Integration)**: [ROLE-MATRIX.md](ROLE-MATRIX.md) §27 표에서 게시판 생성/수정 권한이 없는 역할(예: CenterAdmin 등 제한된 역할)의 토큰으로 게시판 생성 API를 호출했을 때 403으로 거부되는지, 반대로 게시글 등록이 더 넓은 역할/회원까지 허용된 게시판(예: 사용후기형)에서는 해당 역할/회원의 게시글 등록이 정상 처리되는지. **게시판 권한 — 댓글/좋아요(Integration)**: 회원 A가 작성한 댓글을 회원 B의 토큰으로 수정/삭제 시도했을 때 거부되는지(§27 P5 "본인 댓글만 수정·삭제"), 회원 A가 누른 좋아요를 회원 B가 취소(삭제) 시도했을 때 거부되는지(§27 P6 "본인 좋아요만 등록·삭제") — 본인 댓글/좋아요에 대해서는 동일 동작이 정상 처리되는지 positive 케이스도 함께 확인. **첨부파일(Integration)**: `board_posts` 삭제(또는 비활성화) 시 연결된 `files`(`related_entity_type='board_posts'`, `related_entity_id`) 레코드가 함께 처리되는지(고아 파일 방지) — File Manager의 기존 다형 참조 정리 패턴을 재사용하는지 확인. **첨부파일 순서(Unit)**: 다중 이미지 업로드 시 저장된 순서가 화면 표시 순서와 정확히 일치하는지(순서 보존 컬럼/인덱스 존재 여부 확인). **SEO(Unit)**: `content_seo_metadata`(`related_entity_type='board_posts'`)가 게시글 ID 단위로 정확히 1:1 연결되는지, 관리자가 SEO 필드를 직접 입력하지 않으면 제목→Title/본문 요약→Description/대표이미지→OG Image 자동매핑이 적용되는지(BR-053 패턴 재사용 — **신규 BR이 아니라 §2.10에서 이미 검증한 BR-053 원칙을 `board_posts`에 그대로 적용하는 회귀 테스트**), 관리자 수동 입력값이 있으면 항상 그 값이 우선하는지. **예약게시(Integration)**: `board_posts.status=SCHEDULED`이고 `scheduled_publish_at` 도달 시 Scheduler Center Job이 `PUBLISHED`로 전이시키는지 — `products.publish_start_at`(§3.52) 예약 발행과 동일한 Job 패턴인지 회귀 비교(동일한 Scheduler Center 트리거 구조 재사용 여부 확인). **예약게시 경계값(Unit)**: `scheduled_publish_at` 도달 1분 전(미도달)에는 `SCHEDULED` 상태를 유지하며 공개 화면(쇼핑몰/회원몰/마이오피스/메인)에 노출되지 않는지, 도달 즉시 `PUBLISHED`로 전이해 노출되는지의 경계 동작. **승인후게시(Integration)**: `feature_flags.승인후게시=true`인 게시판에서 게시글 등록 시 상태가 즉시 `PUBLISHED`가 아니라 `PENDING_APPROVAL`로 시작하는지, 동시에 Workflow Engine 인스턴스(`workflow_instances.subject_type='BOARD_POST_APPROVAL'`)가 생성되는지, 승인 완료 후에만 `PUBLISHED`로 전이하는지(반려 시 `PUBLISHED`로 전이하지 않는지도 함께 확인). **승인후게시 negative test(Integration)**: `feature_flags.승인후게시=false`인 게시판에서는 게시글 등록 시 `PENDING_APPROVAL` 상태나 `BOARD_POST_APPROVAL` Workflow 인스턴스가 전혀 생성되지 않는지 — §2.5 "Workflow Engine 비흡수 회귀 테스트"와 동일한 성격의 negative 테스트(흐름이 의도치 않게 발동하면 실패). **다국어(Integration)**: `cms_translations`(`content_type='board_posts'`)에 특정 언어 오버레이가 존재하는 게시글은 해당 locale 조회 시 번역된 제목/본문이 반영되는지. **다국어 폴백(Integration)**: 번역이 존재하지 않는 locale로 조회 시 원본(기본 언어) 콘텐츠로 폴백되는지 — [DATABASE.md](DATABASE.md) §3.33이 이미 확립한 "번역이 없는 locale은 기본 언어로 폴백한다" 원칙을 `board_posts`에 그대로 적용하는 **회귀 테스트**(신규 로직 검증이 아님을 명세에 남긴다). **기존 CMS 비영향(Integration, 회귀, 중요)**: Board Engine 도입 전후로 `cms_pages`/`faq_categories`/`faq_items`/`popups`/`banners`의 기존 데이터(행 수, 각 행의 컬럼값)와 그 데이터를 노출하는 기존 화면(공지사항/FAQ/팝업/배너 영역)이 전혀 변경되지 않았는지 — API 응답/화면 렌더링 스냅샷을 Board Engine 도입 전후로 비교해 diff가 없어야 통과. 이 테스트는 §2.5(Workflow Engine 5개 전용 구조 비흡수)·§2.8(ERP Core 의존성 역전 방지)과 동일한 "구조적 가드레일" 성격의 negative 회귀 테스트로, Board Engine 관련 코드가 향후 변경될 때마다 CI에서 상시 실행해 두 구조(기존 CMS ↔ 신규 Board Engine)가 실수로 합쳐지거나 한쪽이 다른 쪽에 영향을 주지 않는지 지속적으로 감시한다 |

### 2.14 한국 공제조합 연동·E-Wallet·글로벌 결제 테스트 (D-075 신규)

| 항목 | 내용 |
|---|---|
| **무엇을** | 공제조합 항목 전송(`compliance_transmission_items`)의 item_type(회원/후원관계/매출/수당/환불/반품/취소)별 전송 대상 식별과 재전송 시 `retry_count` 증가, 공제번호/공제증서 등록(`compliance_member_registrations`)의 status 전이(등록대기→등록완료/등록실패→해지)와 직접판매공제조합/한국특수판매공제조합 동시 등록 시의 독립 추적, E-Wallet `wallet_transactions`(append-only 원장)의 불변성과 `member_wallets`(available/pending/withdrawable/used/hold 5종 잔액 캐시)의 파생 정확성, 출금 워크플로우(REQUESTED→HOLD→APPROVED/REJECTED→RELEASE/COMPLETED→WITHDRAWAL_COMPLETED)의 원장 행 생성과 Workflow Engine(`subject_type='WALLET_WITHDRAWAL'`) 전용 인스턴스 분리, 정산↔지갑 경계(지갑 EARN이 `settlement_items` 금액을 인용만 하고 자체 계산을 수행하지 않는지) 무변경 회귀, 글로벌 결제 Webhook 수신(`payment_webhook_events`)의 인바운드 기록과 기존 §18(아웃바운드) 방향 분리, `external_api_connections.country_code` 기반 PG 라우팅, 그리고 공제조합/E-Wallet/글로벌 결제 각각의 Tenant별 선택 기능 노출 격리 |
| **왜** | [DATABASE.md](DATABASE.md) §3.59~§3.61(D-075)은 세 기능 모두 "전부 Tenant별 선택 기능"이며 신규 연동/정산 엔진을 만들지 않고 기존 구조(API Center/Scheduler Center/Workflow Engine/Payment)를 재사용하도록 설계했다 — 따라서 회귀 위험은 새 계산 로직이 아니라 **재사용 지점에서 기존 원칙이 깨지는 것**에 집중된다. 특히 §3.60이 "잔액은 항상 원장에서 파생하며 직접 UPDATE하지 않는다"고 명시한 것은 §2.2(정산 append-only 원장 불변성)와 정확히 동일한 원칙을 지갑 도메인에 그대로 적용한 것이므로, §2.2가 이미 확립한 테스트 패턴(UPDATE/DELETE 거부 Integration 테스트)을 동일 강제 수준으로 `wallet_transactions`에도 적용해야 한다. 또한 [DECISIONS.md](DECISIONS.md) D-075 결정문은 "후원수당 1차 산정·35% 법적 한도 검증·세금 계산(정산 ③~④단계)은 전혀 변경하지 않았다 — 지갑 적립은 정산이 이미 확정한 금액을 인용하는 추가 지급 채널일 뿐"이라고 명시하므로, 이는 §2.5(Workflow Engine 비흡수 회귀)·§2.13(기존 CMS 비영향 회귀)과 동일한 "비흡수/무변경 가드레일" 성격의 회귀 테스트가 필요한 지점이다. 출금 승인이 Workflow Engine(`subject_type='WALLET_WITHDRAWAL'`)을 재사용하면서도 [SETTLEMENT-RULES.md](SETTLEMENT-RULES.md) §9 ⑤단계(운영자 승인, 정산 전용 구조 유지)와는 완전히 별개 인스턴스여야 한다는 점도 §2.5가 이미 검증한 "5개 전용 구조 비흡수" 패턴과 동일한 negative test 성격이다. 마지막으로 `payment_webhook_events`(인바운드)는 기존 `external_api_call_logs`(아웃바운드, §18)와 방향이 반대이므로, 두 흐름이 혼동되면 결제 데이터 정합성에 직접 영향을 준다 |
| **어떻게(레벨)** | **공제조합 항목 전송(Unit)**: 회원/후원관계/매출/수당/환불/반품/취소 각 item_type이 `compliance_transmission_items`에서 정확히 전송 대상으로 식별되는지. **공제조합 항목 전송(Integration)**: 전송 실패 후 재전송 시 `retry_count`가 정확히 증가하고 새 `external_api_call_logs` 행이 생성되는지 — §2.7 Idempotency 원칙과 유사하게, 동일 항목이 status=성공인 상태에서 재전송 요청이 들어와도 중복 전송(중복 `external_api_call_logs` 생성)이 발생하지 않는지 확인. **공제번호/공제증서 등록(Unit, 경계 케이스)**: `compliance_member_registrations.status` 전이(등록대기→등록완료/등록실패→해지)가 정의된 경로 외의 전이(예: 해지→등록대기 역행)를 거부하는지. **공제번호/공제증서 등록(Integration)**: 동일 회원이 직접판매공제조합과 한국특수판매공제조합 두 곳에 동시 등록 시, 두 등록 행이 서로 독립적으로 상태를 추적하며 한쪽의 상태 변경이 다른 쪽에 영향을 주지 않는지. **E-Wallet append-only 원장 불변성(Integration)**: `wallet_transactions`에 대한 UPDATE/DELETE가 애플리케이션 레벨(repository/service 계층)에서 거부되는지 — **§2.2의 "append-only 원장 불변성" 테스트와 정확히 동일한 원칙이며, `commission_records`/`settlement_items`와 같은 강제 수준(메서드 비노출 또는 호출 거부, 필요 시 DB 권한/트리거 강제)으로 검증해야 한다.** **E-Wallet 잔액 파생 정확성(Unit)**: `member_wallets`의 5종 잔액 캐시(available/pending/withdrawable/used/hold)가 해당 회원·통화의 `wallet_transactions` 전체를 정확히 집계한 값과 일치하는지. **E-Wallet 잔액 파생 정확성(Integration)**: 캐시와 원장이 의도적으로 불일치하는 시나리오(캐시 재계산 테스트)를 주입했을 때, 시스템이 캐시값이 아니라 원장 재집계 값을 항상 옳은 값으로 채택하는지 — §2.3이 "캐시(Redis)는 추정치, Postgres 누적값이 법적 판단 근거"로 구분한 것과 동일한 "캐시 vs 원본" 우선순위 원칙. **출금 워크플로우(Integration)**: REQUESTED 시 HOLD 원장 행 생성(available 감소, hold 증가) → APPROVED/REJECTED 분기 → REJECTED 시 RELEASE 원장 행(available 복원) → COMPLETED 시 WITHDRAWAL_COMPLETED 원장 행(hold 소진)이 매 전이마다 정확한 금액으로 생성되는지. **출금 워크플로우 — Workflow Engine 비흡수 회귀(Integration, negative test)**: Workflow Engine(`subject_type='WALLET_WITHDRAWAL'`)이 §2.5가 검증하는 기존 5개 전용 승인구조(조직 이동/생애주기/프로그램 신청/포인트 사용신청/정산 운영자 승인)를 흡수하지 않는지와 동일한 패턴으로, 지갑 출금 승인 인스턴스가 정산 운영자 승인(§9 ⑤단계, [SETTLEMENT-RULES.md](SETTLEMENT-RULES.md))과 `subject_type`/`subject_id`가 혼동되지 않고 완전히 별개 워크플로우 인스턴스로 생성·처리되는지 확인. **정산↔지갑 경계 회귀(Integration, 중요)**: 후원수당 1차 산정/35% 법적 한도 검증/세금 계산(§2.1/§2.3 기존 테스트가 이미 다루는 영역)이 E-Wallet 도입 전후로 전혀 변경되지 않았는지(§2.5/§2.13과 동일한 "비흡수/무변경" 가드레일 성격), 지갑 EARN 트랜잭션의 금액이 `settlement_items`가 이미 확정한 금액을 그대로 인용만 하고 자체 계산을 수행하지 않는지 — **분배 정책(O-201, 정산↔지갑 적립 분배)이 미확정이므로 본 항목은 §2.7/§2.9/§2.11/§2.12/§2.13과 동일하게 O-201 확정 후에야 구체화 가능한 명세 수준이다.** **글로벌 결제 Webhook 수신(Integration)**: `payment_webhook_events`가 인바운드 수신 시 정확히 기록되는지(status 수신/처리중/처리완료/처리실패 전이), `signature_verified=false`일 때의 처리 방식(거부/격리)은 **O-205(Webhook 서명검증 방식) 확정 전까지는 명세 수준**임을 명시. **글로벌 결제 Webhook 방향 분리(Unit, 회귀)**: `payment_webhook_events`(인바운드)와 기존 §18(`order_payment_attempts`, 결제 PG 시도 상태, 아웃바운드)의 호출 방향이 코드/테스트에서 명확히 분리되어 있는지 — 인바운드 처리 로직이 아웃바운드 시도 상태를 직접 변경하거나 혼동하지 않는지 확인하는 방향 혼동 방지 회귀 테스트. **PG country_code 스코프(Integration)**: 동일 Tenant가 여러 국가(예: 한국/태국/일본)의 PG를 `external_api_connections.country_code`로 동시에 등록했을 때, 주문의 배송지/결제국가에 맞는 PG로 정확히 라우팅되는지 — 국가 매칭 실패 시 폴백 동작도 함께 확인. **Tenant별 선택 기능 격리(Integration, 중요, 전체 라운드 공통)**: 공제조합 연동/E-Wallet/글로벌 결제 각각을 Tenant A는 활성화하고 Tenant B는 비활성화한 상태에서, Tenant B의 화면/API 응답에 해당 기능 메뉴·엔드포인트·데이터가 전혀 노출되지 않는지 — **Multi-Tenant 데이터 행 자체의 격리는 여전히 D-035 "구조 준비, 활성화 보류" 상태(§2.9 참조)이므로, 본 테스트는 "기능 가시성(노출 여부)" 레벨에 한정되고 "데이터 행 격리" 레벨 검증은 §2.9가 명시하는 대로 Multi-Tenant 활성화(O-090) 이후 범위임을 명시한다.** |

## 3. 영역 간 우선순위 권고

구현 착수 시 테스트 작성 우선순위는 다음 순서를 권고한다 — 모두 [DO-NOT-TOUCH.md](DO-NOT-TOUCH.md)/[ARCHITECTURE.md](ARCHITECTURE.md)가 "위반 금지"로 명시한 영역일수록 우선한다.

1. **35% 법적 한도 게이트**(§2.3) — 법령 hard constraint 위반은 가장 심각한 등급의 리스크
2. **MLM 수당 계산 worked-example 회귀**(§2.1) — 이미 한 차례 실제 회귀(D-014→D-018)가 발생했던 영역
3. **append-only 원장 불변성**(§2.2) — 위반 시 사후 복구가 사실상 불가능한 데이터 손상
4. **Idempotency**(§2.7) — 중복 지급은 직접적 금전 손실
5. **Workflow Engine 비흡수 회귀**(§2.5) — 향후 무관한 기능 추가 중 실수로 깨지기 쉬움
6. **API 계산 분리**(§2.6) — 구조적 원칙이며 직접적 금전 손실보다는 아키텍처 건전성 문제
7. **쇼핑몰/주문**(§2.4) — 일반적인 CRUD/흐름 검증, 다른 영역보다 회귀위험이 상대적으로 낮음
8. **ERP Core 의존성 역전 방지**(§2.8) — D-046 원칙이 깨지면 영향 범위가 넓지만, 발생 빈도는 낮은 구조적 리스크
9. **Multi-Tenant 격리**(§2.9) — Multi-Tenant 활성화(O-090) 전까지는 우선순위가 낮음, 활성화 결정 시 1순위로 재배치
10. **쇼핑몰 운영 Phase 2/SEO/Digital Marketing**(§2.10) — §2.4(쇼핑몰/주문)와 동일한 낮은~중간 우선순위 — 금전적 직접 영향은 없으나 검색엔진 노출/소셜 공유 품질에 영향을 주는 회귀(특히 BR-053 우선순위 위반)는 출시 전 점검 필요
11. **쇼핑몰 UX/알림/운영자 대시보드**(§2.11) — 전반적으로 §2.4/§2.10과 동일한 운영/UX 성격의 낮은~중간 우선순위(장바구니/관심상품/최근 본 상품/상품 비교/알림 템플릿/대시보드 지표/업무 Queue) — 다만 그 안에서도 **결제 실패 재시도**(`order_payment_attempts`, STATE-MACHINE.md §18)와 **자동결제 실패 알림**(§19)은 결제 재시도 흐름과 직접 맞물려 있어 나머지 하위 영역보다 한 단계 높게 점검한다
12. **운영 UX 및 고객 경험**(§2.12) — Abandoned Cart/Saved Cart/Customer Timeline/My Dashboard/SEO·Feed 오류 위젯은 금전 직접 영향이 없는 UX 편의 영역으로 전체 중 가장 낮은 우선순위이나, 그 안의 **Quick Action 무회피 테스트**만은 예외로 §2.5(Workflow Engine 비흡수 회귀)와 동일한 우선순위 등급(승인 우회 방지 회귀)으로 점검한다
13. **Dynamic Board Engine**(§2.13) — 신규 관리자용 CMS 확장 기능으로 금전 직접 영향이 없어 §2.10~§2.12와 유사한 낮음~중간 우선순위이나, 그 안의 **기존 CMS 비영향 테스트**와 **승인후게시 negative test**만은 예외로 §2.5(Workflow Engine 비흡수 회귀)/§2.8(ERP Core 의존성 역전 방지)과 동일한 우선순위 등급(구조적 가드레일)으로 점검한다
14. **한국 공제조합 연동·E-Wallet·글로벌 결제**(§2.14) — 그 안에서도 **E-Wallet append-only 원장 불변성 테스트**와 **정산↔지갑 경계 회귀 테스트**만은 예외로 §2.2/§2.3과 동일한 최상위 우선순위 등급(법령/금전 직결 — 사후 복구 불가능한 데이터 손상 또는 정산 계산 오염 위험)으로 점검하고, 나머지(공제조합 항목 전송/등록, Webhook 수신, PG country_code 라우팅, Tenant 기능 격리)는 §2.10~§2.13과 유사한 중간 우선순위로 점검한다

## 4. Open Items (본 문서 범위에서 확정하지 못한 사항)

- O-054(`idempotency_key` 도입 범위) 확정 전까지 §2.7의 테스트는 명세 수준에 머문다.
- §2.6의 "api 응답시간 임계치" 정확한 수치는 운영 데이터 확보 전까지 미정— 구현 단계에서 실측 후 보정 필요.
- O-004(35% 초과 시 정확한 처리 방식 — 배치 전체 보류/비례 축소/마지막 항목만 보류)가 확정되어야 §2.3의 "차단 이후" 동작에 대한 테스트 케이스를 구체화할 수 있다 — 현재는 "③→④ 전이 보류"까지만 테스트 가능.
- O-159/O-170(Multi-Tenant Job 격리/온보딩) 확정 전까지 §2.9는 "단일 테넌트 가정" 테스트만 작성 가능 — 활성화 결정 시 본 절을 구체화해야 한다.
- 재고 예약(O-127)/카테고리 구조(O-128)/반품 상태머신(O-129)이 미확정이므로, §2.4 쇼핑몰 테스트의 재고·반품 관련 케이스는 해당 Open Decision 확정 후 추가한다.
- O-078(실시간 캐시↔Postgres 정합화 Job 주기)이 확정되어야 §2.3의 drift 보정 테스트의 정확한 시간 윈도우를 정할 수 있다.
- O-195(검색엔진 지표 캐시 저장 vs 실시간 API 호출)가 확정되어야 §2.10의 "SEO Dashboard 지표 테스트"를 구체화할 수 있다 — 확정 전에는 등록/상태 관리 레벨 테스트만 작성 가능하다.
- O-197(장바구니 "나중에 구매하기" 시 `cart_items` 삭제 vs 상태 보존)이 확정되어야 §2.11의 "관심상품 테스트"(장바구니→관심상품 이동) 케이스를 구체화할 수 있다.
- O-198(가격인하 알림 트리거 기준 — 절대가 vs 할인율)이 확정되어야 §2.11의 "가격인하 알림 테스트"의 경계값 케이스를 작성할 수 있다 — 확정 전에는 등록/발송 자체의 동작 레벨 테스트만 작성 가능하다.
- O-192(Abandoned Cart/첫구매·생일쿠폰 자동발급 트리거 시점과 책임 모듈 — Scheduler Center vs 쇼핑몰 모듈)가 확정되어야 §2.12의 "Abandoned Cart 자동 쿠폰 테스트"를 완전히 구체화할 수 있다 — 확정 전에는 발급 결과(`coupon_issuances` 기록·중복방지)의 동작 레벨 테스트만 작성 가능하다.
- O-200(기존 CMS 콘텐츠의 Board Engine 통합 마이그레이션 여부)이 확정되기 전까지는 기존 CMS(`cms_pages`/FAQ/팝업/배너)와 신규 Board Engine(`boards`/`board_posts`)이 두 개의 병렬 구조로 공존해야 한다 — 이것이 §2.13의 "기존 CMS 비영향 테스트"가 단순 참고용이 아니라 **마이그레이션 결정 전까지 상시 점검이 필요한 가드레일**인 이유다. O-200이 통합 방향으로 해소되면 §2.13의 회귀 테스트 범위 자체를 재정의해야 한다.
- O-201(정산↔지갑 적립 분배 정책 — 전액/회원선택/병행)이 확정되어야 §2.14의 "정산↔지갑 경계 회귀 테스트"를 명세 수준에서 실제 테스트 케이스로 구체화할 수 있다 — 확정 전에는 "지갑 EARN이 `settlement_items` 금액을 그대로 인용만 한다"는 원칙 검증 레벨에 한정된다.
- O-205(결제 Webhook 서명 검증 방식 및 검증 실패 시 처리 — 거부/격리 후 검토)가 확정되어야 §2.14의 "글로벌 결제 Webhook 수신 테스트" 중 `signature_verified=false` 처리 분기의 정확한 동작을 테스트할 수 있다 — 확정 전에는 인바운드 기록 자체의 동작 레벨 테스트만 작성 가능하다.
